localStorage는 **문자열만** 저장할 수 있어서:

- **저장할 때**: `JSON.stringify(객체)` → 문자열로 변환
- **가져올 때**: `JSON.parse(문자열)` → 객체로 복원

즉, 정리해서 로컬스토리지는 문자열만 저장할 수 있기 때문에 저장할 때는 JSON.stringify로 문자열로 변환을 해서 저장을 해야하고, 가져올 때는 JSON.parse를 이용하여 객체로 복원하여 가져와야한다.
